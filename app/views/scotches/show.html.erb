<%- self.page_title = h(@scotch.name) %>

<dl>
  <dt>Age</dt>
  <dd><%= h(@scotch.age) %></dd>
</dl>

<h3>Have you had this scotch? What say you?</h3>

<%- if signed_in? %>
  <%- semantic_form_for [@scotch, @note] do |form| %>
    <%= form.error_messages %>
    <%= form.inputs :body %>
    <%= form.buttons :commit %>
  <% end %>
<% else %>
  <p>We can't wait to hear about it, but... you gotta <%= link_to "Sign in", sign_in_path(:return_to => request.path) %> or <%= link_to "Sign up", sign_up_path(:return_to => request.path) %> first.
<% end %>


<h3>Here's what others are saying</h3>

<%- if @scotch.notes.present? -%>
  <%= render :partial => 'notes/list_item', :collection => @scotch.notes %>
<%- else %>
  <p>Er... or how about this... you could be the first!</p>
<%- end %>
